# UplinkIRC

A fast, secure, IRCv3-featured IRC client built with Qt6 and C++.

## Building

```bash
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build
```

Requires Qt6 (Core, Widgets, Network).

## Status

Early development — scaffold stage.
